def spiral_square(n):
    grid = [[0 for j in range(n)] for i in range(n)]

    lst = []
    return lst

def print_square(S):
    for i in range(len(S)):
        print(' '.join([(2*' '+str(e))[-3:] for e in S[i]]))

exec(input().strip())

# print_square(spiral_square(3))